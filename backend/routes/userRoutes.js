// backend/routes/userRoutes.js
const express = require('express');
const router = express.Router();
const User = require('../models/user');

// Define user routes (e.g., registration, login, etc.)

module.exports = router;
